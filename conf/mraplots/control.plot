Control vs Desired

Heading: ${EstimatedState.psi}
Desired Heading: ${DesiredHeading.value}
Depth: ${EstimatedState.depth}
Altitude: ${EstimatedState.alt}
Desired Z: ${DesiredZ.value}
Roll: ${EstimatedState.phi}
Desired Roll: ${DesiredRoll.value}
