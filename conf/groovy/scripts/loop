
println "hello world"
while(true) {
	//println vehicles.length==0 ? "no vehicles" : vehicles_id.length
	vehicles.each {println "VEHICLE: "+it}
	plans.each {println "PLAN: "+it}
	Thread.sleep(30000)
}
println "done"